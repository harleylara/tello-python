from setuptools import setup, find_packages

setup(
    name='tello',
    version='0.0.1',
    description='Tello Drone Camera Interface',
    packages=find_packages(),
    install_requires=[
        'traitlets'
    ],
) 
